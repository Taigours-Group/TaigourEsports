# TODO

## Purchase request 400 (Bad Request) fix
- [ ] Update `components/PlayerBalance.jsx` to surface backend error details and add client-side validation.
- [ ] Update `server.js` `/api/purchase-request` endpoint to return more specific 400 payload (which field is missing/invalid).
- [ ] Re-test recharge + membership submissions and verify no 400s.

